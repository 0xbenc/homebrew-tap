# 0xbenc Homebrew Tap

Homebrew formulas for 0xbenc projects.

## ssherpa

Install:

```sh
brew tap 0xbenc/tap
brew install ssherpa
```

Or install directly:

```sh
brew install 0xbenc/tap/ssherpa
```
